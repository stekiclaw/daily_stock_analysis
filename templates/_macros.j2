{# Shared macros for report templates #}
{% macro market_snapshot(result) %}
{% set snapshot = result.market_snapshot if result.market_snapshot else {} %}
{% if snapshot %}
### 📈 {{ labels.market_snapshot_heading }}

| {{ labels.close_label }} | {{ labels.prev_close_label }} | {{ labels.open_label }} | {{ labels.high_label }} | {{ labels.low_label }} | {{ labels.change_pct_label }} | {{ labels.change_amount_label }} | {{ labels.amplitude_label }} | {{ labels.volume_label }} | {{ labels.amount_label }} |
|------|------|------|------|------|-------|-------|------|--------|--------|
| {{ display_value_or_na(snapshot.get('close')) }} | {{ display_value_or_na(snapshot.get('prev_close')) }} | {{ display_value_or_na(snapshot.get('open')) }} | {{ display_value_or_na(snapshot.get('high')) }} | {{ display_value_or_na(snapshot.get('low')) }} | {{ display_value_or_na(snapshot.get('pct_chg')) }} | {{ display_value_or_na(snapshot.get('change_amount')) }} | {{ display_value_or_na(snapshot.get('amplitude')) }} | {{ display_value_or_na(snapshot.get('volume')) }} | {{ display_value_or_na(snapshot.get('amount')) }} |
{% set snapshot_price = display_value_or_na(snapshot.get('price')) %}
{% if snapshot_price != 'N/A' %}

| {{ labels.current_price_label }} | {{ labels.volume_ratio_label }} | {{ labels.turnover_rate_label }} | {{ labels.source_label }} |
|-------|------|--------|----------|
| {{ snapshot_price }} | {{ display_value_or_na(snapshot.get('volume_ratio')) }} | {{ display_value_or_na(snapshot.get('turnover_rate')) }} | {{ display_value_or_na(snapshot.get('source')) }} |
{% endif %}

{% endif %}
{% endmacro %}
